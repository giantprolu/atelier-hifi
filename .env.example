# Copie en .env et remplis. Le .env est ignoré par git.
# Rappel : ne colle jamais ces valeurs dans un chat, un ticket ou un commit.

R2_ENDPOINT=https://<account_id>.r2.cloudflarestorage.com
BACKUP_BUCKET=atelierhifi-backups
AWS_PROFILE=r2
